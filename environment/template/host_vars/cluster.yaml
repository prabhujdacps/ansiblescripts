#Cluster information
ARM_CLUSTER_NAME : 
ARM_CLUSTER_MULTICAST : Y

#Server group configurations, ARM_CLUSTER_NAME should be empty
ARM_SERVER_GROUP_NAME :

#Server sets, IP ignore if cluster type is server group
ARM_SERVER :
    <NODE> :
         NAME :
         IP:
    <NODE> :
         NAME : 
         IP: 